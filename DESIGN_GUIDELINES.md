# erkeys.com Design Guidelines

이 문서는 erkeys.com 메인, 연락하기, 사용 가이드, MODU-C 키맵/키캡 이미지 및 관련 하위 페이지가 같은 시각 언어를 유지하기 위한 기준이다. 목적은 예쁘게 꾸미는 것이 아니라, 제품 안내와 기술 자료를 조용하고 정확하게 읽히게 만드는 것이다.

## Core Direction

- 배경은 흰색, 글자는 검은색을 기본으로 한다.
- 장식보다 정보 위계를 우선한다. 색, 두께, 크기 차이는 꼭 필요한 만큼만 쓴다.
- 브랜드명 `erkeys 에르키스`는 작고 담백하게 둔다. 제품명 `MODU-C 모두키보드`가 화면의 중심이다.
- 전반적인 인상은 신생 하드웨어 브랜드의 공식 문서처럼 조용하고 단정해야 한다.
- 랜딩 페이지처럼 과장된 히어로, 큰 홍보 문구, 둥근 카드식 구성, 강한 그라디언트, 장식용 도형은 피한다.

## Typography

- 기본 글꼴은 `Pretendard`, 보조는 `SUIT`, 시스템 sans-serif 순서로 둔다.
- 자간은 기본적으로 `letter-spacing: 0`을 유지한다. 좁은 자간이나 과한 벌림은 쓰지 않는다.
- 제목은 너무 크지 않게 유지한다. 현재 기준은 데스크톱 `clamp(1.92rem, 4vw, 2.7rem)`, 모바일 `clamp(1.52rem, 6.2vw, 1.95rem)`이다.
- 중간 강조 문장은 `clamp(1.02rem, 2vw, 1.16rem)`, 본문은 `0.9rem` 전후를 기준으로 한다.
- 작은 브랜드/내비게이션/푸터 텍스트는 `0.8rem`-`0.82rem` 범위에서 통일한다.
- 굵기는 보통 `600`-`700` 사이만 사용한다. 같은 문장 안에서 불필요하게 굵기를 쪼개지 않는다.
- 행간은 제목 `1.15`, 강조 문장 `1.45`, 본문 `1.65`-`1.7`을 기준으로 한다.

## Layout And Density

- 본문 폭은 `min(100%, 560px)`을 기본 단위로 한다.
- 데스크톱 여백은 좌우 `24px`, 상하 `72px`을 기준으로 한다.
- 모바일 여백은 좌우 `20px`, 상하 `44px`을 기준으로 한다.
- 첫 화면은 너무 빽빽하지 않게 둔다. 핵심 제목, 한 줄 설명, 짧은 본문, 1-3개 행동 버튼 정도가 적정 밀도다.
- 여백 기준:
  - 작은 라벨과 제목 사이: `16px`
  - 제목과 리드 문장 사이: `24px`
  - 리드 문장과 설명 사이: `14px`
  - 설명과 버튼 묶음 사이: `30px`
  - 하단 보조 정보 전 여백: `76px`-`92px`
- 버튼은 높이 `42px`, 내부 여백 `11px 16px`, 버튼 간격 `10px`을 기준으로 한다.
- 박스형 정보 영역은 꼭 필요할 때만 사용하고, 둥근 모서리 없이 직선적인 형태를 유지한다.
- 텍스트 줄 길이는 한국어 기준 한 줄이 과도하게 길어지지 않게 `max-width: 520px` 전후로 묶는다.

## Color And Hierarchy

- 기본 색:
  - 배경: `#ffffff`
  - 본문: `#000000`
  - 보조 텍스트: `#555555`
  - 더 낮은 보조 텍스트: `#777777` 또는 `#999999`
  - 옅은 구분선: `#eeeeee`
- 주요 버튼은 검은 배경과 흰 글씨로 둔다.
- 보조 버튼은 흰 배경, 검은 테두리, 검은 글씨로 둔다.
- 비활성 항목은 `#999999` 텍스트와 `#dddddd` 테두리 정도로 낮춘다.
- 경고/상태 박스는 아주 옅은 배경을 먼저 깔고, 얇은 선으로만 강조한다. 현재 연락 페이지 기준은 배경 `#fff6f6`, 선 `#d82121`이다.
- 색상만으로 의미를 전달하지 말고 텍스트/상태도 함께 둔다. 다만 설명 문구는 과하게 붙이지 않는다.

## Keycap Image Direction

- MODU-C 키맵/키캡 이미지는 사이트와 같은 흰 배경, 검은 선, 회색조 위계를 따른다.
- 키보드는 실제 제품 자료처럼 위에서 내려다본 정돈된 구성이어야 한다.
- 키캡 라벨은 중앙 정렬을 기본으로 하되, 보조 레이어 표기가 필요하면 작은 글씨로 일정한 위치에 둔다.
- 키캡 사이 간격은 실제 키보드 구조가 읽힐 정도로 분명해야 하지만, 장난감처럼 큼직하거나 헐겁게 보이면 안 된다.
- 선 두께와 그림자는 최소화한다. 입체감보다 키 위치와 라벨 가독성이 먼저다.
- 컬러 강조가 필요하면 회색조 단계로 시작한다. 기본, hover/focus, pressed, layer-active 상태가 서로 구분되면 충분하다.
- 이미지 안의 글자는 Pretendard/SUIT 계열 느낌을 유지한다. 너무 두껍거나 둥근 디스플레이 폰트는 피한다.
- 완성 이미지는 데스크톱과 모바일 폭에서 라벨이 뭉개지지 않는지 확인한다.

## Interactive Keymap Page

- 최종 결과물은 정적 HTML/CSS/JS로 만든다. 새 프레임워크나 빌드 도구를 추가하지 않는다.
- 키 입력 시 해당 키가 회색조로 반응해야 한다. 기본 상태, 눌림 상태, 레이어 활성 상태가 시각적으로 구분되어야 한다.
- `lower` 키는 특히 기술 검토 대상이다.
  - 실제 펌웨어 키맵에서 lower 키가 어느 위치와 어떤 동작 이름으로 정의되어 있는지 확인한다.
  - 브라우저 `keydown` 이벤트만으로 실제 하드웨어의 lower 레이어 동작을 완전히 감지할 수 있는지 검토한다.
  - 감지가 제한된다면 화면상의 lower 키 클릭/터치와 키보드 입력 fallback을 분리해 구현하고, 한계를 문서화한다.
  - lower가 눌린 상태에서 다른 키 라벨 또는 키 상태가 레이어 기준으로 바뀌는지 테스트한다.
- 반응형 페이지에서는 키보드 전체가 화면 밖으로 튀어나가지 않아야 한다. 작은 화면에서는 비율 축소 또는 가로 스크롤 중 하나를 명확히 선택한다.
- 접근성을 위해 각 키는 식별 가능한 텍스트 또는 `aria-label`을 가진다.

## Review Workflow

1. 키캡 이미지 담당자는 이 양식으로 이미지를 1회 렌더한다.
2. 렌더 결과를 erkeys.com 메인 페이지 담당자에게 전달해 검토받는다.
3. 메인 페이지 담당자는 다음 조건을 까다롭게 확인한다.
   - Pretendard/SUIT 계열 인상 유지
   - 글자 크기 과대 사용 없음
   - 자간 `0`에 가까운 안정감
   - 흰 배경/검은 글자/회색조 중심
   - 적정 밀도와 여백
   - 키캡 라벨 가독성
   - 제품 자료처럼 단정한 형태
4. 조건을 만족할 때까지 이미지를 반복 수정한다.
5. 승인된 이미지를 기준으로 실제 반응형 HTML 페이지를 만든다.
6. 키 입력, 회색조 반응, lower 키 레이어 반응을 기술 검토한다.
7. 완성된 HTML을 erkeys.com 위키 세팅 담당자에게 넘겨서 키세팅 위키 페이지 본문 내부의 하위 페이지 링크로 첨부하게 한다.

## Acceptance Criteria

- 전체 인상이 메인 페이지, 연락하기, 사용 가이드와 같은 브랜드로 보인다.
- 제목과 키캡 라벨이 크거나 진해서 촌스럽게 튀지 않는다.
- 여백이 충분하지만 내용이 헐거워 보이지 않는다.
- 데스크톱과 모바일에서 텍스트가 겹치거나 잘리지 않는다.
- 키 입력 반응이 눈에 띄지만 과하게 화려하지 않다.
- lower 키 동작은 실제 구현 가능성과 한계를 검토한 기록이 남아 있다.
- 위키에 연결할 수 있는 정적 HTML 산출물이 준비되어 있다.
